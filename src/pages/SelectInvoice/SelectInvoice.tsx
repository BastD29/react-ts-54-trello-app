import { FC } from "react";

const SelectInvoice: FC = () => {
  return (
    <main className="select-invoice" style={{ padding: "1rem" }}>
      <p>Select an invoice</p>
    </main>
  );
};

export default SelectInvoice;
