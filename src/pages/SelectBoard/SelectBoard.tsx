import { FC } from "react";

const SelectBoard: FC = () => {
  return (
    <main className="select-board" style={{ padding: "1rem" }}>
      <p>Select a board</p>
    </main>
  );
};

export default SelectBoard;
